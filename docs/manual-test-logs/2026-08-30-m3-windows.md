# M3 Windows manual test log — 2026-08-30

Run of [`docs/manual-tests.md`](../manual-tests.md) against the Windows backend.
The matrix is the canonical procedure; this file is the log. A row is **PASS**
only when the expected result was observed on real hardware.

| Field | Value |
|---|---|
| Date | 2026-08-30 |
| Commit | `ba0abed` (main, clean tree) |
| Build | `cmake --preset default` (RelWithDebInfo), UCRT64 MinGW |
| Core | `keygnosys-core 0.1.0` |
| Backends | `windows-hook` / `windows-sendinput` / `windows-win32` |
| Capabilities | `suppress`, `warp_absolute`, `move_windows` |
| OS | Windows 11 Home 10.0.26200 |
| Monitors | 3 — see rig below |
| Operator | Johhannas Reyn (physical actions), Claude (commands, evidence capture) |

## Monitor rig

| Device | Primary | Bounds |
|---|---|---|
| `\\.\DISPLAY1` | yes | x=0, y=0, 5120×1440 |
| `\\.\DISPLAY2` | no | x=758, y=1440, 3840×1080 |
| `\\.\DISPLAY3` | no | **x=-3082**, y=1440, 3840×1200 |

`DISPLAY3` has a negative x origin, so §6.3 — the row that catches the classic
primary-only-normalisation bug — is genuinely exercisable on this rig.

---

## Pre-flight (before any row was run)

### Emergency exit — verified

Three independent exits, listed strongest first.

| # | Exit | Depends on | Verified |
|---|---|---|---|
| E1 | `Ctrl+Alt+Del` → Task Manager → End task | **Nothing of ours.** Windows guarantees the SAS is not interceptable (matrix row 8.3 documents this as a limitation; here it is the safety net). Mouse-only after the SAS. | Mechanism is OS-guaranteed |
| E2 | Desktop shortcut **KEYGNOSYS PANIC** — double-click, mouse only | Kills `keygnosys-core`, then force-releases every modifier (both sides) and all three mouse buttons via `keybd_event`/`mouse_event` | ✅ executed clean |
| E3 | `release_all` / `set_enabled false` over IPC, issued by Claude | Core healthy. Per SPEC §5.4 a reply means **applied**, not merely accepted | ✅ both replied `ok:true` |

`Escape` → `layer.release` is bound in `default.json` and is the *normal* in-layer
exit, not an emergency one — it depends on the engine being healthy, which is the
thing under test.

**Why E2 exists.** Killing the core removes the hook, so physical keys work again
— but it does **not** undo a `SendInput` key-down that was never matched with an
up. E2 covers that case; kill alone does not.

### Finding P-1 — the documented emergency exit is not sufficient (matrix defect)

`docs/manual-tests.md` Setup says to keep a terminal open with
`taskkill /IM keygnosys-core.exe /F` ready. **That exit cannot be used under the
conditions it exists for.** While the layer is latched, `layer_engine.cpp:302-305`
swallows every unbound non-modifier key, so no letter reaches any window — the
command cannot be typed. `Ctrl+C` is equally unavailable: `Ctrl` is forwarded as a
modifier (`:297-300`) but `KeyC` is swallowed.

Severity: the matrix's stated safety mechanism is inert in the latched state.
Proposed fix: replace it with E1/E2 above.

### Finding P-2 — the Setup block crashes on this machine (matrix defect)

The Setup block runs the binary with no PATH prefix:

```sh
cmake --preset default && cmake --build --preset default
./build/default/core/keygnosys-core.exe
```

Followed literally in MSYS2 bash, this segfaults **12/12** before printing its
banner. Cause is DLL resolution, not a product defect: the binary is UCRT64-linked
(imports `api-ms-win-crt-*`) but `/mingw64/bin` precedes `/c/msys64/ucrt64/bin` on
this PATH, so it loads MINGW64's msvcrt-based `libstdc++-6` / `libgcc_s_seh-1` /
`libwinpthread-1`. The `debug` build survives the mismatch; the optimized build
does not.

| Launcher | debug | default (RelWithDebInfo) |
|---|---|---|
| MSYS2 bash, no PATH prefix | 5/5 OK | **5/5 SEGV** |
| MSYS2 bash, `PATH=/c/msys64/ucrt64/bin:$PATH` | — | 5/5 OK |
| Native Windows launch | 5/5 OK | 5/5 OK |

The repo's own verification commands already carry the `PATH=` prefix; only the
manual-test Setup block omits it. Proposed fix: add the prefix there too.

### Finding P-3 — row 2.8 asserts a requirement that does not exist

Row 2.8 expects a CapsLock press under 30 ms to be swallowed ("the layer toggles
once, not twice"). There is **no debounce in the engine** (`grep debounce core/src/`
returns only the unrelated 250 ms `windows` event debounce) and **no debounce in
the SPEC** — §6.3 never mentions one, and 30 ms appears nowhere in the repo.

The row therefore tests neither the code nor the spec. It is also not reliably
performable by hand. Recommendation: strike the row, or promote it to a new
requirement with a SPEC change first. **Not run as a product pass/fail.**

### Prerequisites confirmed

| Need | Status |
|---|---|
| Build, both presets | ✅ green; 14/14 ctest both presets; `pytest` 90 passed |
| PySide6 (§12 overlay rows) | ✅ 6.11.2 in `.venv` |
| ≥2 monitors, one at negative origin (§6, §7) | ✅ see rig |
| IPC client for §10/§11 command rows | ✅ scratch harness, correct `{v,t,n,id,d}` envelope |
| `hello` reports real backends (§11.1) | ✅ captured above |
| `hello.limitations` carries both entries (§8.5) | ✅ captured verbatim |

### Rows needing a decision before they run

| Row | Blocker |
|---|---|
| 9.2 | Registry edit of `LowLevelHooksTimeout` + **reboot**. Disruptive; recommend deferring. |
| 8.1 | Needs an elevated window → UAC prompt. |
| 6.6 | Change display arrangement live, then restore. |
| 6.7 | Change per-monitor scaling to 100% / 150%, then restore. |
| 3.2, 3.5 | Depend on hitting a 50 ms window by hand; expect retries. |

---

## Results

Legend: **PASS** / **FAIL** / **NOT RUN** / **N/A**

### 1. Ordinary input is unaffected

| # | Result | Evidence |
|---|---|---|
| 1.1 | NOT RUN | |
| 1.2 | NOT RUN | |
| 1.3 | NOT RUN | |
| 1.4 | NOT RUN | |
| 1.5 | NOT RUN | |
| 1.6 | NOT RUN | |
| 1.7 | NOT RUN | |

### 2. CapsLock and the activation modes

| # | Result | Evidence |
|---|---|---|
| 2.1 | NOT RUN | |
| 2.2 | NOT RUN | |
| 2.3 | NOT RUN | |
| 2.4 | NOT RUN | |
| 2.5 | NOT RUN | |
| 2.6 | NOT RUN | |
| 2.7 | NOT RUN | |
| 2.8 | **N/A** | Finding P-3: tests a requirement absent from both code and SPEC |

### 3–12

NOT RUN — see below.

---

---

## Finding D-1 — BLOCKER: the hook is installed but never receives callbacks

Execution stopped at section 1. Rows 1.2 onward are **NOT RUN**.

### Symptom

`keygnosys-core` emits **zero `key` events** for real typing. Also zero
`modifiers` events, and the cursor layer never engages. Ordinary typing is
completely unaffected — which is why row 1.1 *looked* like a pass.

### Evidence

| # | Observation |
|---|---|
| 1 | A 240 s capture spanning a typed sentence recorded exactly **1 line** (`hello`). |
| 2 | Reproduced across **two independent client implementations** (Python `open()`, .NET `NamedPipeClientStream`). Not a client artifact. |
| 3 | **Control experiment:** an independent `WH_KEYBOARD_LL` hook in a separate process observed **94 callbacks** over the same keyboard, first event `vk=0x53 scan=0x1F flags=0x00` — a non-injected `KeyS`, a scancode our table maps. Low-level hooks work fine on this machine. |
| 4 | **Overlapping-window proof.** The core's own capture ran 08:04:26 → ~08:14:26. The control experiment ran 08:08:44 → 08:09:05, entirely inside it, and proved 94 real keystrokes. The core's capture over that window totals **1 line** (`hello`). Same keyboard, same seconds, 94 callbacks to an independent hook and 0 to ours. |
| 5 | Live debugger inspection: `installed_=true`, `hook_=0x4f04115b` (valid `HHOOK`), `hookLosses_=0`, `admissionRefusals_=0`, `enabled_=true`, `ready_=true`, `stopping_=false`. |
| 6 | Hook thread alive and parked in `NtUserMsgWaitForMultipleObjectsEx` at `hook_input.cpp:257` — the wait in `run()`. |
| 7 | `Core::step()` runs every tick and calls `drainPublication()` unconditionally (`core.cpp:1151`); `windows` events were observed streaming, proving the loop turns. The publication ring is simply always empty. |
| 8 | The control path works end-to-end: `set_activation_mode` replies `ok:true` and `get_state` confirms it applied. `submitAndWait` blocks on the hook thread's `applied_` event, so **the hook thread wakes, drains control and signals back**. |

> **Evidence note added 2026-09-02.** Rows 1, 2 and 4 of the table above were
> gathered with `watch.ps1`, which was later found to go deaf after its first
> line (see the 2026-09-02 section). Their *zeros* were therefore true for the
> wrong reason, and row 2's "not a client artifact" claim was unsound at the time
> it was made. **The finding itself stands independently**: the counter probe
> measured `trampoline=0` under the `INFINITE` wait and `trampoline=1816` with the
> wait capped, on counters that never passed through any IPC client. D-1 is
> confirmed by that probe, not by the captures.

### Boundary

Everything up to and including the hook thread's control handling is healthy.
`onHook` itself never produces a `PhysicalRecord`. Since `publishPhysical` is
called on every path that gets past the two early returns, either the callback is
never invoked, or it returns at the `LLKHF_INJECTED` / `!key.valid()` guards
(`hook_input.cpp`). The keymap is ruled out: `test_scancode_keymap` round-trips
100+ codes and the control experiment's first event was scancode `0x1F`, which the
table maps to `KeyS`.

### RESOLVED 2026-09-01 — H1 confirmed

Temporary uncommitted instrumentation added counters to `HookInput` and changed
exactly one behaviour: `waitTimeout()` returning `50` instead of `INFINITE` at
idle. No other change. Counters are atomics; nothing was logged, allocated or
blocked inside the callback, and reporting happens from the hook thread's loop
outside the callback.

Human-confirmed window: operator was asked to type continuously for 10 s and
replied "done".

| Counter | Before window | After window | Delta |
|---|---|---|---|
| Independent-hook callbacks | — | **529** | 529 (first `vk=0x20 scan=0x39 flags=0x00`) |
| `trampoline` | 115 | 1931 | **+1816** |
| `hcAction` | 115 | 1931 | **+1816** |
| `injected` (skipped by design) | 9 | 328 | +319 |
| `validKey` translated | 106 | 1589 | **+1483** |
| `invalidKey` | 0 | 14 | +14 |
| `PhysicalRecord` attempted | 106 | 1589 | **+1483** |
| `PhysicalRecord` enqueued | 106 | 1589 | **+1483** |
| `hookLosses` | 0 | 0 | 0 |

Install facts: `hhook=0x5b251bd1`, `module=0x7ff6ee320000` (the exe's own base),
`GetLastError=0`, `installAttempts=1`, `installOk=1`, pid `31036`, hook thread
`34416`.

**Conclusion: H1.** The single change of capping the idle wait made callbacks
appear. Installation was never the problem — the same arguments, module handle
and thread produced zero callbacks with `INFINITE` and 1816 with `50 ms`.
`recAttempt == recEnq` exactly, so the publication ring never dropped anything;
`invalidKey=14` out of 1589 is ordinary unmapped-key traffic, not a translation
failure. H2 is excluded.

### Original hypothesis (now confirmed)

`waitTimeout()` returns `INFINITE` whenever no grace deadline is pending, which is
the steady state. The thread then blocks in `MsgWaitForMultipleObjectsEx` and
never reaches the `PeekMessageW` on the next line — which the comment at `:261`
identifies as what actually dispatches the hook. The comment at `:255-256` asserts
"`QS_ALLINPUT` is what lets the system call the hook while we are here"; that
assumption is what needs testing.

Evidence 8 complicates this: the thread demonstrably does reach `PeekMessageW`
whenever a control arrives. A poke test (399 control messages at 20 Hz, forcing
~400 `PeekMessageW` calls) produced no key events either — but it is **not known
whether keys were pressed during that window**, so that run is inconclusive, not a
refutation.

The design note's §4 chose this single-primitive wait deliberately ("One
primitive does both jobs"). If the hypothesis holds, that choice is the defect.

### Not a confound

Windhawk (`windhawk.dll`, Engine 1.7.3) is injected into the core process
(thread 2, `WindhawkMainLoopThreadAttachExempt`). It was considered and
**excluded**: the control experiment's own hook, running under the same Windhawk,
received all 94 callbacks.

---

## Status

**BLOCKED at row 1.1.** One row observed, one blocker found.

| Row | Result |
|---|---|
| 1.1 | PASS as written (typing unaffected) — but see D-1: it passes *because* nothing is intercepted |
| 11.1 | PASS — `hello` reports `windows-hook` / `windows-sendinput` / `windows-win32`, not mock |
| 8.5 | PASS — both limitations present verbatim |
| 7.4 | PASS — 10 windows observed, tenth carried `index: null`, no existing slot moved |
| 2.8 | N/A — Finding P-3 |
| all others | NOT RUN |

Row 1.1's pass is hollow: with no interception, "ordinary input is unaffected"
is vacuously true. It must be re-run after D-1 is fixed.

**M4 is not started and must not start.** The M3 Windows backend does not
intercept input on real hardware.

---

# 2026-09-02 — permanent fix verified live

## What the repair was

`GetMessageW` replaces `MsgWaitForMultipleObjectsEx` as the hook thread's
blocking primitive. Control wakeups became coalesced `PostThreadMessageW`
doorbells over the same SPSC ring; the grace deadline became a one-shot
`SetTimer`. See `kgn/hookpump.hpp` and the corrected §6 of the threading spec.

## A measurement error that cost several rounds

Between the fix landing and it being confirmed, five separate runs reported
**zero** `key` events from a core that was in fact working correctly. The cause
was the capture tool, not the product: `watch.ps1` (a `StreamReader.Peek()` loop
under Windows PowerShell 5.1) received the `hello` line and then went deaf while
the process stayed alive, so it looked like a healthy observer reporting a real
negative.

It was caught by triggering `mode`/`modifiers`/`focus` broadcasts from a second
connection and observing that a *foreground* client received them while the
background capture — still running — recorded nothing. A Python client validated
against the same known broadcasts captured all of them.

Two conclusions were drawn from the bad instrument and are **withdrawn**:

- "The bug is inside `HookInput`, not the pump" — wrong; it came from the broken
  watcher reading 0 while the observers read 384.
- "The `PeekMessage`+`Sleep` observer undercounts" — disproved by direct test:
  `get=384` and `peek=384`, identical over the same keystrokes.

Lesson recorded in the matrix: **validate an instrument against a known-positive
signal before trusting a negative from it.**

## Deterministic comparison (item 6)

Measured with a self-triggering harness — the operator arms and stops the count
from the keyboard with `F12`, so no window alignment with anyone else is needed.
The harness's state machine is self-tested against a canned stream (pre-arm noise
ignored, trigger excluded, stray keys segregated).

### Part 1 — ten discrete taps of `A`

| Measure | Predicted | Observed |
|---|---|---|
| KeyGnosys `KeyA` events | 20 | **20** — 10 `down`, 10 `up` |
| suppressed split | 10 / 10 | **10 true / 10 false** |
| stray keys inside the window | 0 | **0** |
| Independent hook, injected | 10 | **10** |

The suppressed split is the grace-expiry path, not a defect: at ~1.2 s per tap
each hold exceeded the 50 ms window, so each **down** was buffered and suppressed,
the timer expired and forwarded it synthetically — which is the observer's 10
injected events — and each **up** then passed through natively.

**Duplicate physical callbacks are ruled out.** 10 taps produced exactly 20
events, not 40.

### Part 2 — one three-second hold of `A`

| Measure | Observed |
|---|---|
| `down` | **1** |
| `repeat` | **55** |
| `up` | **1** |
| suppressed | 1 true, 56 false |

55 repeats over 4.28 s ≈ 12.9/s, matching the system repeat rate. Autorepeat
stays on the native passthrough path, which is the property that makes the
physical-bitmap `Repeat` derivation load-bearing.

### The 529-vs-1497 discrepancy — resolved

Window misalignment, not duplication. The 529 came from the observer's own 90 s
window; the 1497 from a KeyGnosys counter delta spanning a longer, unsynchronised
period, with injected events filtered on one side only. The two were never
like-for-like. When both are aligned by construction — as above — they agree
exactly, and the independent hook's 32 non-injected events are fully accounted
for by keys pressed outside the trigger window that our count correctly excluded.

## Automated verification after the repair

| Check | Result |
|---|---|
| Windows `debug` (strict, `-Werror`) | build clean, **15/15** |
| Windows `default` | build clean, **15/15** |
| Linux (WSL Ubuntu 24.04, `-Werror`) | build clean, **12/12** (3 Windows-only excluded) |
| `pytest` | **90 passed** |
| `git diff --check` | clean |
| Idle wakeups | hook thread **0.000 s CPU over 10 idle seconds**; core's 60 Hz motion loop 0.234 s |

## Still outstanding

**Hook-loss detection does not work.** `installed_` is our own flag and nothing
clears it when *Windows* removes a hook, so a long-lived process can hold a
stale-but-valid-looking `HHOOK` and report itself healthy. This is pre-existing,
untouched by this repair, and means matrix **row 9.1 cannot pass as written**.
Not fixed here: it is a separate defect and this change is a pump repair.

---

# 2026-09-02 — row 9.1 investigation: hook loss NOT reproduced

Investigated before implementing recovery, on the principle that a watchdog for
an undemonstrated failure mode is speculation with a retry loop attached.

## Documented Windows semantics

`LowLevelKeyboardProc` Remarks (learn.microsoft.com, retrieved 2026-09-02):

> "The hook procedure should process a message in less time than the data entry
> specified in the **LowLevelHooksTimeout** value in the following registry key:
> `HKEY_CURRENT_USER\Control Panel\Desktop`. The value is in milliseconds. If the
> hook procedure times out, the system passes the message to the next hook.
> However, on Windows 7 and later, the hook is silently removed without being
> called. **There is no way for the application to know whether the hook is
> removed.**"

> "**Windows 10 version 1709 and later** The maximum timeout value the system
> allows is 1000 milliseconds (1 second). The system will default to using a 1000
> millisecond timeout if the **LowLevelHooksTimeout** value is set to a value
> larger than 1000."

`UnhookWindowsHookEx` documents only nonzero/zero plus `GetLastError`; it says
nothing about a handle the system has already removed, and it is destructive, so
it cannot serve as a health probe.

**Therefore Microsoft states plainly that no affirmative detection API exists.**
Any detection must come from observing delivery, not from asking Windows.

## Reproduction procedure

Scratch experiment (`hookloss.cpp`, never committed — it deliberately blocks
inside a hook callback, the one thing production code must not do).

Two hooks, two threads, one process:

- **victim** — overruns the timeout once, deliberately, via `Sleep()` in its callback
- **control** — never blocks, own thread, own `GetMessageW` pump

The control exists so "no callbacks" can never be confused with "the operator
stopped typing". Both counters are read at the same instant in the same process,
so the windows are aligned by construction. An `F12` start gate on the control
hook lets the operator begin when ready.

Machine: Windows 11 Home 10.0.26200. `LowLevelHooksTimeout = 5000` (explicitly
set in `HKCU\Control Panel\Desktop`).

## Results

| Run | Overrun | victim after | control after | `UnhookWindowsHookEx` | Verdict |
|---|---|---|---|---|---|
| 1 | 2500 ms | **+680** | +680 | returned 1, err=0 | survived |
| 2 | 6500 ms | **+606** | +606 | returned 1, err=0 | survived |

6500 ms exceeds both the documented 1000 ms cap and the machine's configured
5000 ms. In both runs the victim and control counts are **identical**, so every
event that reached the control also reached the victim: the victim was never
removed. The successful unhook with `GetLastError=0` independently confirms the
handle was still live.

**Silent removal did not occur on this build under a single overrun.** The
documented 1000 ms cap also appears not to be in force, since a 2500 ms overrun
did not trip a supposedly 1000 ms ceiling.

## Consequences

**No watchdog, sentinel, or recovery state machine was implemented.** Building
one now would mean designing detection for behaviour we cannot demonstrate,
against an API Microsoft says cannot report it.

**A retraction.** The earlier suggestion that the 2.5-hour-old core had a
silently removed hook is **withdrawn as unsupported**. It was offered as
plausible and never confirmed; the demonstrated cause of that episode was the
deaf `watch.ps1` capture client, and hook removal has now failed to reproduce
deliberately.

**The code defect is nevertheless real, and is narrower than first stated.**
`installed_` is cleared only by our own `uninstall()`. The reinstall branch in
`run()` is therefore reachable *only* for an install that never succeeded — not
for a hook removed after succeeding. Its comment claims otherwise, and
`hookLosses_` can only count the former, so the `input.hook_lost` diagnostic in
SPEC §11 is unreachable in practice. That is a truthfulness problem (P6), not a
missing watchdog.

## Row 9.1 status

Row 9.1 accepts either branch: "Either the hook survives, or it is removed and
re-installed automatically with an `input.hook_lost` diagnostic."

On this machine the hook **survives**, which satisfies the first branch. The
second branch is not implementable without a detection mechanism the platform
does not provide. Recommend rewriting the row to state what is actually
guaranteed, rather than leaving an alternative that cannot be delivered.

Not yet run as a formal matrix row: it needs the load conditions in its own
procedure, not this synthetic overrun.

---

# 2026-09-03 — sections 1–3 on the repaired implementation

Run against the `GetMessageW` pump plus the high-resolution waitable timer.
`grace_ms = 50` throughout unless stated.

## Section 1 — ordinary input

| Row | Result | Evidence |
|---|---|---|
| 1.1 | PASS | 553 key events, 266 down / 265 up, operator confirmed no drops, doubles or felt latency |
| 1.2 | PASS | `KeyA`: 1 down + **81 repeat** + 1 up, 83/84 unsuppressed — native autorepeat |
| 1.3 | PASS | `ShiftLeft` ×11 with `modifiers` events, capitals confirmed |
| 1.4 | NOT RUN | No IME installed on this machine |
| 1.5 | PASS | `ControlLeft` ×65, `AltLeft` ×4, 48 `focus` changes; copy/paste and Alt+Tab confirmed |
| 1.6 | NOT RUN | No injected-input-sensitive application available. Native passthrough makes a pass *likely*, which is exactly why it is not recorded as one |
| 1.7 | PASS | Media/vendor key produced no `key` event — the correct signature for an unmapped key — and operator confirmed it did its job |

**Note on the grace cost.** About a third of ordinary keystrokes are suppressed
and replayed, because every action-bound key is buffered while the layer is off.
The operator could not perceive it at a measured ~71 ms.

## Section 2 — CapsLock and activation modes

| Row | Result | Evidence |
|---|---|---|
| 2.1–2.3, 2.6, 2.8 | PASS | Operator observation; recording confirms CapsLock suppressed when driving the layer and forwarded for the Shift escape gesture. Segments were not row-aligned (ordinary typing interleaved), so the capture is supporting rather than primary evidence |
| 2.4 | PASS | toggle: `MODE cursor latched=True` on press, ~60 autorepeats with **no further flips**, release emits nothing |
| 2.5 | PASS | hold: engages on press, drops on release; a tap leaves it off |
| 2.7 ⚠ | PASS | Layer left while `KeyL` held: the release stayed suppressed (obligation discharged, not leaked), the next press behaved natively, all modifiers false afterwards |

## Section 3 — the grace window

| Row | Result | Evidence |
|---|---|---|
| 3.1 | PASS | `J down → replay → J up`; the replay is the typed character |
| 3.2 @ 50 ms | PASS, with a caveat | 5 clean one-to-one attempts, **0 leaks**. Every `delta_t` was **negative** (CapsLock leading J by 0–16 ms), so the grace window itself was never exercised — with CapsLock first the layer is already engaged when J arrives |
| 3.3 | PASS | Letters typed first, layer engaged afterwards |
| 3.4 | PASS | Initial replay then 8 physical repeats, **none re-injected** — autorepeat stays native |
| 3.5 | **NOT EXERCISED** | CapsLock always arrived 130 ms–1.7 s after the three keys, far outside the window. The one near-simultaneous case had the layer already latched, so the keys ran as actions without touching the buffer |
| 3.6 @ 50 ms | PASS | median 62.9 ms, min 51.8, max 69.2, **0 early expiries** in 11 samples |
| 3.6 @ 25 ms | measured | median 34.2 ms, min 27.8, max 49.3, **0 early expiries** in 11 samples |

### Method failures worth recording

Three analysis errors were made before section 3 produced usable data, none of
them product defects:

1. A solo-J grace replay was counted as a 3.2 leak. It is row 3.1 working.
2. A ±100 ms pairing window let one CapsLock press partner several J presses,
   inventing 21 attempts from 10. Fixed with strict one-to-one nearest pairing.
3. `F12` segment delimiters were assumed to be present; only 2 of 10 arrived,
   because the operator types replies on the same keyboard and floods the log.

The general lesson, already recorded in the matrix: validate an instrument
against a known-positive before believing a negative from it.

### `grace_ms` recommendation

**Keep 50 ms.** The operator's natural chord puts CapsLock 0–16 ms *ahead* of the
bound key, an ordering that never touches the grace window. Narrowing it would
buy ~28 ms of latency the operator cannot perceive, at the cost of margin for the
J-first ordering — which ~15 natural attempts failed to produce even once. That
is the trade SPEC §15.10 exists to prevent. Doing this properly needs the M5
calibration UI and 20–30 samples, not hand analysis.

## Section 4 — pointer motion

All PASS. Verified from the `pointer` event stream rather than by eye where
possible:

| Row | Result | Evidence |
|---|---|---|
| 4.1, 4.4, 4.6 | PASS | Operator observation; single-pixel nudging confirmed usable |
| 4.2 | PASS | `right` held spans median 484 → max 5509 px/s: the ramp is real |
| 4.3 | PASS | `down+right` max 3930 px/s against cardinal max ~5000–5500. Diagonal is **not** √2 faster, so normalisation works |
| 4.5 | PASS | `precision+right` max 1033 px/s vs `right` max 5509 |
| 4.7 | PASS | Pointer x ranged **−3082 … 5119**, spanning `DISPLAY3`'s negative origin to the right edge of `DISPLAY1`. Negative virtual coordinates handled |

## Section 5 — buttons and scrolling

| Row | Result |
|---|---|
| 5.1, 5.2, 5.6, 5.7, 5.8 | PASS (operator observation) |
| **5.3** | **FAIL — see D-2**, then **PASS** after the fix; see D-2 resolution |
| 5.4, 5.5 ⚠ | NOT RUN — drag lock deferred; they are the P7 rows and deserve a fresh session |

### Finding D-2 — `button.double_click` misses, and the SPEC is why

**Symptom.** Double-click frequently registers as two single clicks ("no click"),
and sometimes as a triple.

**Cause.** `Core::scheduleDoubleClick` emits the first pair immediately and
schedules the second at `now + doubleClickInterval()`, where the Windows backend
returns `GetDoubleClickTime()` — **410 ms on this machine, 500 ms by default**.

`GetDoubleClickTime()` is the *maximum* gap the OS will accept as a double-click,
not the gap to use. Scheduling at exactly that value lands on the threshold, and
`serviceDoubleClicks()` runs on the 60 Hz core loop, so the delivered gap is
`interval + 0…16.7 ms` — **over the limit**. Windows then sees two independent
single clicks.

The triple follows: an attempt that appears to do nothing invites a second press,
and two delayed second-clicks then land around the new first click.

**The code faithfully implements a SPEC sentence that is wrong.** §7.2 says the
pairs are *"separated by the OS double-click interval"* — it should be **within**
it — and asserts *"the interval is tens of milliseconds"*, which is off by an
order of magnitude.

**Ruled out.** Autorepeat is not a contributor: `layer_engine.cpp:269` suppresses
a repeated press of a key already driving an action, so a held `X` cannot queue
extra clicks.

**Proposed fix, not applied.** Schedule the second pair at a fraction of the
interval — `interval / 4`, or a small fixed delay around 60 ms — and correct both
§7.2 sentences. Needs a test asserting the scheduled gap is strictly less than
the backend's reported interval, which would have caught this.

### D-2 resolved — 2026-09-04

Fixed in `7424c35`. `doubleClickDelay()` now waits `min(interval / 4, 80 ms)`,
clamped to leave a tick of jitter margin, instead of the OS interval itself.
SPEC §7.2 corrected on both counts.

Re-measured with a `WH_MOUSE_LL` observer, so the row rests on a number rather
than on how it felt:

| | before | after |
|---|---|---|
| delivered gap | 410–427 ms | **90.5 – 94.2 ms** |
| against the 410 ms threshold | at or over | **315.8 ms inside**, worst case |
| injected button-downs per gesture | — | **exactly 2** across 7 gestures, so nothing stacks |

**Row 5.3: PASS.** Operator confirmed the word selects every time, with no
misses and no triples.

The pair-count matters as much as the gap: 14 downs for 7 gestures rules out the
stacking that produced the triple-click symptom.

---

# 2026-09-04 — section 5 completed, a P7 defect, and the operator-free rows

Instruments: the core's own event stream (`tools/manual/record.py`) and an
independent `WH_MOUSE_LL` observer (`tools/manual/observe_mouse.ps1`). The
second is not optional here — a drag lock's button-down is synthesized, and
synthetic events are invisible to the core's own stream by design
(`hook_input.cpp` skips `LLKHF_INJECTED`), so only an independent hook can see a
button left physically down.

**Pre-check.** `kgn_hook_smoke` PASS — `code=1 state=1 suppressed=0`. A first
launch returned FAIL and was **discarded as inconclusive**: the operator had not
yet been told to type, so no stimulus was applied. A zero from an instrument
whose stimulus was never applied is not a negative.

**Instrument validated before use.** The mouse observer recorded a deliberate
physical click (known-positive) before any row was believed — 8 physical events,
first at 16.54 s. Only then was its report about injected events trusted.

## Row 5.4 — drag, move, drop — PASS

| Measure | Observed |
|---|---|
| Injected button events | 4 — two balanced `D`/`U` pairs |
| Hold durations | 3.499 s and 28.345 s |
| Net down count | **0** |
| File dragged and dropped | operator confirmed |

Two cycles were run rather than one; both balanced. Both halves of the row were
observed — the capture proves button state, the operator confirms the file
actually moved. Neither alone is the row.

## Row 5.5 — auto-release on layer exit — FAIL

### Finding D-3 — drag lock does not auto-release when the layer is left (P7)

**Symptom.** The left mouse button stayed physically down for **9.215 s after
the layer was left**, released only because the operator happened to press `G`
again. Found by accident: the operator ran the gesture twice, and the second
lock was still active at the exit.

**Evidence.** From the core's own stream:

```
1043.312  KeyG down    -> drag_lock {left, active: true}    button locks down
1051.425  CapsLock down
1051.594  mode normal, latched=false                        LAYER LEFT
             ... no drag_lock release. Button still down.
1055.439  F12
1059.040  CapsLock     -> layer re-engaged
1060.809  KeyG down    -> drag_lock {left, active: false}   released only now
```

Corroborated by the independent `WH_MOUSE_LL` observer. Its four injected events
map one-to-one onto the four core `drag_lock` events with a clock-offset spread
of **0.007 s**, so the mapping is not an artifact of either instrument:

| core `drag_lock` | mouse observer injected | offset |
|---|---|---|
| 1028.135 D | 2009.39 D | 981.255 |
| 1040.520 U | 2021.77 U | 981.250 |
| 1043.312 D | 2024.56 D | 981.248 |
| 1060.809 U | 2042.06 U | 981.251 |

**Root cause.** `Dispatcher::releaseAll` (`actions.cpp:469`) is the only code
that clears `dragLock_`, and its own comment cites the SPEC §7.2 P7 clause. It
was called from exactly three places — `Dispatcher::setBindings`,
`Core::Impl::releaseEverything`, `Core::stop` — **none of which is layer exit**.
Layer exit runs `LayerEngine::leaveCursorMode` (`layer_engine.cpp:551`), which
unwinds held action keys only; a drag lock is deliberately not a held action, so
nothing was emitted for it. `LayerEngine` (hook thread) and `Dispatcher` (core
thread) communicate only through the `WorkRing`, and no decision or work-item
kind meant "the layer was left" — the signal had nowhere to travel. The comment
documented an intent the wiring never delivered.

**Test gap.** `test_actions.cpp` `release_all_lifts_every_obligation` calls
`Dispatcher::releaseAll` directly and asserts the drag lock lifts. It proved the
mechanism and never the wiring — the same shape as this milestone's original
defect, where the whole suite was green while the hook thread retrieved no
messages.

**A defect in this matrix's own instrumentation.** The planned check for these
rows was `net down count == 0`. That check **passes on this failure**: the
button does come up, 9.2 s late and for the wrong reason. A net-balance check
cannot see a release that happens at the wrong *time*. Rows 5.4 and 5.5 need the
event *sequence*, not just the balance. Recommend the plan's verification step
be rewritten accordingly.

### D-3 fix — implemented and unit-verified, NOT yet re-run live

Fixed in `57d3cd2`, tests corrected in `dc3f1bd`. A layer-exit signal now
crosses the work ring: `leaveCursorMode` emits one `LayerExited` decision when
the layer was actually engaged, `translateDecisions` turns it into a
`ReleaseToggles` work item, and the core thread releases the dispatcher's drag
locks and applies the button effects. The signal is emitted *after* the
held-action unwind, so a `button.click` still holding the same button releases
its refcount first and exactly one up-edge is issued rather than a spurious
down/up pair.

The work-ring overflow proof in `hookchannel.hpp` was updated rather than left
contradicting the code: `kMaxLayerExitWork = 1`, `kMaxReleaseWork` 384 → 385,
`static_assert` updated, prose rewritten to argue why at most one such item can
appear in a drain.

Coverage of every exit path is structural rather than enumerated by hand:
`mode_ = Mode::Normal` is assigned in exactly one place in the whole engine, so
no exit can bypass the signal. The regression test was confirmed to fail at each
of the three links independently (engine, channel, core).

**Row 5.5 remains FAIL in this log.** The fix is verified by tests, not by
observation. The defect was found live and the closing evidence must come from
the same place — the operator ended the session before the re-run. Re-running
row 5.5 against `dc3f1bd` is the first task of the next session.

## Rows completed without operator keystrokes

These need no physical input, so they were run directly.

| Row | Result | Evidence |
|---|---|---|
| 7.1 | PASS | 11 windows; the 9 real slots carried unique stable indices 1–9 |
| 7.4 | PASS (re-confirmed) | 12 windows; indices 1–9 unchanged, the excess reported with `index: null`, no learned slot moved |
| 8.5 | PASS | `hello.limitations` read from the wire carries **three** entries verbatim: elevated-window inertness, `Ctrl+Alt+Del` never interceptable, and silent hook removal being undetectable |
| 9.2 | PASS | The third limitation is present and worded as a platform constraint, not a defect. This row is recordable, not executable |
| 11.3 | PASS | Second core exits **3** with `ipc.endpoint_in_use: another process already owns \\.\pipe\keygnosys`; first core (pid 54564) undisturbed |
| 11.9 | PASS | **2029** `key` events analysed across real typing. Every payload carried exactly `{code, state, suppressed}` — 0 with any other field. All 47 distinct codes positional (`KeyA`, `Digit0`, `ArrowDown`…). No character content crossed the wire (SPEC §12.4) |
| 7.2 | **DIVERGENCE — see M-1** | Remaining windows kept every index; the freed slot did **not** stay empty |
| 7.3 | NOT RUN | Cannot create a free slot without closing windows in the operator's live workspace. All nine slots held real work and two windows were already in overflow |

### Monitor topology baseline (for section 6)

| index | device | primary | bounds |
|---|---|---|---|
| 0 | `\\.\DISPLAY1` | yes | x=0, y=0, 5120×1440 |
| 1 | `\\.\DISPLAY3` | no | **x=−3082**, y=1440, 3840×1200 |
| 2 | `\\.\DISPLAY2` | no | x=758, y=1440, 3840×1080 |

Confirmed from `get_state`: a negative-origin monitor exists, so row 6.3 is
genuinely exercisable on this rig. Section 6 itself needs keystrokes and was not
run.

### Finding M-1 — row 7.2 expects behaviour the SPEC does not require (matrix defect)

Row 7.2 expects "The remaining windows keep their indices. **The freed slot is
empty.**"

Observed: closing the window in slot 5 left indices 1,2,3,4,6,7,8,9 untouched —
the property the row exists to protect — but slot 5 was immediately backfilled
by a Paint.NET window that had been `index: null`.

This is the implementation behaving sensibly, not a defect. `slots.cpp:19-21`
assigns any window "not currently in a slot" to the lowest free index; it does
not distinguish a newly-opened window from an existing overflow one. SPEC §6.5
rule 3 says only that *"a new window takes the lowest free index"* and is silent
on what happens to an overflow window when a slot frees. The alternative —
leaving the slot dead — would strand overflow windows as permanently
unreachable even as slots free, which is worse, and backfilling displaces no
index the user has learned (the window that owned it is gone).

Severity: the row asserts something stricter than either the SPEC or the design
intends, so it cannot pass whenever more than nine windows are open.
Recommendation: **change the SPEC and the row together.** SPEC §6.5 rule 3
should read "a window with no slot takes the lowest free index", and row 7.2
should expect the freed slot to be empty *only when no overflow window exists*.
Not fixed here: changing slot semantics is a design decision, and this milestone
has twice found that the right move is to correct the specification rather than
bend the code to a row.

## Session end

The operator ended testing here. Nothing was left stranded: all three mouse
buttons read up via `GetAsyncKeyState`, and the mouse observer recorded 452
physical events with **0 injected** and a net down count of 0 for the session
after the rebuild.

Remaining and genuinely blocked on physical input: rows 5.5 (re-run), 6.1–6.7,
7.3, 7.5–7.11, 8.1–8.4, 9.1, 9.3, 10.1–10.9, 11.2, 11.4–11.8, 12.1–12.4.
Injected keystrokes cannot substitute: the hook skips `LLKHF_INJECTED` by
design, so no synthesized key can drive the layer. These rows require a human at
the keyboard and there is no way around that.
